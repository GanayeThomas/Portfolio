/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      remotePatterns: [
        {
          hostname: 'dwocmoxpqnsmtiipqvmu.supabase.co',
        },
      ],
    },
  };
  
  export default nextConfig;